import UIKit

// the place user goes when they tap on a cell on the StoreSfeed.
class StoreDeals: UIViewController {
        override func viewDidLoad() {
        view.backgroundColor = .red
        print("DealsVC")
    }
}
